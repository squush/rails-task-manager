<h1> Update -<%= @task.title %>-</h1>

  <%= form_with model: @task do |t| %>
    <%= t.label :title %>
    <%= t.text_field :title %>
    <%= t.label :details %>
    <%= t.text_field :details %>
    <%= t.submit %>
  <% end %>

<%= link_to "Back to #{@task.title}", task_path(@task) %>
